# Work plan — 2026-07-18 — core/fms migration

## Product version

**v4.4.3**

## Completed (same day cascade)

1. **v4.4.1** — `core/indicators` (`ema` / `returns_pct` / `r_squared_3m`) + pre-filter keep
2. **v4.4.2** — `core/tradeability` (`calculate_tradeability_filters`)
3. **v4.4.3** — `core/fms` (`_mom_snapshot` / `compute_fms_snapshot` / `momentum_now_and_delta`)
   - Also moved `ytd_return` / `last_vol_annualized` into `core/indicators`
   - `analysis_utils` remains identity re-export facade for all of the above

## How FMS is validated

| Layer | Harness |
|-------|---------|
| Golden ranks / -999 / no-network | `tests/unit/test_fms_scoring.py` |
| Shim identity | `test_analysis_utils_reexports_core_fms` |
| Manual table | `python -m harness.run_fms_snapshot` |
| core boundary | `tests/contract/test_no_network_in_core.py` |

## Baseline verification

```text
python -m pytest              # 31 passed
python -m harness.run_fms_snapshot
```

Golden FMS order unchanged: TREND_UP > MILD_UP > FLAT > CRASHY(-999).

## Next

1. Recalib formula fork removal: `f_current` / `f_proposed` → call `core.fms`
2. Optional: `get_filter_debug_info` → `core/tradeability`
3. Mid-term: `universe_utils` / `watchlist_utils` → adapters; app.py duplicate downloads
